plugins {
    // Auto-provisions the JDK declared by the toolchain below if it is not installed locally.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "spruce-settings"
